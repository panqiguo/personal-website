这是一篇没有 frontmatter 的随笔示例。

你只需要新建一个 `.md` 文件就可以发布文章。标题会默认使用文件名，日期默认使用文件创建时间（或修改时间）。

如果需要图片，可以在同目录建一个同名文件夹，然后相对路径引用：如果需要图片，可以在同目录建一个同名文件夹，然后相对路径引用如果需要图片，可以在同目录建一个同名文件夹，然后相对路径引用如果需要图片，可以在同目录建一个同名文件夹，然后相对路径引用如果需要图片，可以在同目录建一个同名文件夹，然后相对路径引用
如果需要尾注[^1], 试试

![随笔配图](./今天的随笔/paper-note.svg "small")

这是一个外链示例：[Astro](https://astro.build)。

这是一个内链示例：[Writing](/writing)。


[^1]: 这是第一个尾注
